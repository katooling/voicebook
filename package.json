{
  "name": "@voicebook/local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "voicebook": "./src/cli.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test --test-concurrency=1 tests/*.test.ts",
    "test:browser": "node --experimental-strip-types --test --test-concurrency=1 tests/browser.test.ts",
    "test:install-browser": "playwright-core install chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "playwright-core": "^1.61.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24.2"
  }
}
